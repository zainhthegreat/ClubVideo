enum FileType {
  VIDEO
  PHOTOS
  TEXT
}

type File @model @auth(rules: [{allow: public}, {allow: owner}]) {
  id: ID!
  Name: String!
  Type: FileType!
  category: String!
  description: String
  ownerID: String!
  Grade: Int!
}
 